# 3.2.5.24：队长手动分配后一键下发

在歌曲自动接收功能上增加第二种用法，随 3.2.5.24 在线发布。自动分配保持可用；不增加演奏中切换乐器功能。

## 全队准备

全队通过在线库升级至 3.2.5.24 或相同的后续兼容版本。开发插件用户需完整解压发布包并重新指定包内 MidiBard2.dll，停用旧开发版；这个 ZIP 是卫月插件，不是 Penumbra 模组。详细步骤见 LOCAL-SONG-SYNC.md。

全队开启“多设备演奏”“合奏监听”“跨电脑当前歌曲同步”。游戏小队队长创建演出房间，队员使用“队员查看邀请”连接同一房间。跨公网沿用已有 TCP 隧道。只需主控有曲库，队员可为空曲库或保留不同顺序的个人曲库。

## 队长操作

1. 停止当前合奏，打开原 MidiBard 合奏面板。
2. 关闭队长端的“自动分配乐器与演奏人”。队员无需更改自己的这个开关。
3. 在原播放列表双击歌曲。手动模式下先在队长本机载入，尚未向全队下发。
4. 在合奏面板为每条启用的轨道选择演奏人、乐器和移调。不使用的轨道取消勾选。
5. 点击“下发歌曲与手动分配”。队员缺少 MIDI 时接收到独立缓存，然后按本次手动分配载入并取出指定乐器。
6. 等待面板显示“全员已载入歌曲与手动分配，可以准备合奏”，再点原合奏准备按钮。

修改轨道、乐器或人员后需重新下发。即使 MIDI 已经缓存，新一轮下发也会重新获取配置，不会复用旧分配。有人进出小队或更换队长后，也需重新确认并下发。

## 自动与手动两种模式

| 用法 | 队长设置 | 选曲后的行为 |
| --- | --- | --- |
| 自动分配 | 开启自动分配；队员同样开启 | 按原流程下发 MIDI，依据队长小队显示顺序自动分配 |
| 手动分配 | 关闭队长的自动分配 | 先在队长端编辑，点击下发按钮后同步歌曲与手动方案 |

队员对本次手动下发使用队长方案，优先级高于队员本地的自动分配、轨名配器和同名歌曲旁的配置；不会永久更改队员的自动分配开关。切回自动模式后，由队长重新选曲。

手动配置会沿用原合奏面板的本地保存机制。自动点歌中的“合奏主控”也能使用当前歌曲或已保存的有效手动配置：载入并校验后再下发、等待回执和准备。新歌尚未完成配置时会停下提示，先回合奏面板完成分配。队长接管后发送手动配置仍需原房主在线，歌曲文件的可用范围沿用原房间分发机制。

## 同步内容与限制

- 同步 MIDI 本体，以及轨道启用状态、演奏人、乐器、移调、当前速度和越界音符适配设置；方案绑定本次下发、MIDI 内容及当前小队。手动模式的电吉他音色按队长选择的轨道乐器固定，防止 MIDI 程序事件覆盖指定音色。
- 每个启用轨道必须指定当前小队中的演奏人和有效乐器。一个人可分配多轨，但这些启用轨道必须使用同一乐器；FF14 单人演奏能力不因多轨分配而增加。
- 支持最多 100 条有音符的轨道，单曲仍限 16 MiB。MIDI 下载上限 90 秒，手动模式队长等待全员载入最多 180 秒。
- 未分配的队员不会自动接替别人的声部。演出人员和乐器需要满足游戏本身的合奏条件。
- 只在载入成功并完成乐器准备后回报成功；有成员失败时，插件不允许以本次配置发起准备。取消、断线、换队长或名单变化会使进行中的操作失效。
- 不复制歌词、完整旁文件 JSON 或默认演奏人文件；接收配置仅用于当前播放，不覆盖个人曲库文件和排序。
- 所有下发均在演奏开始前完成。实时游戏音符输出和原合奏计时流程保持原有机制。

## 建议双机验收

选一首两轨 MIDI，刻意把“轨名建议的乐器”改成另一种乐器，把第一轨分给队员、第二轨分给队长，并为一轨设置移调。队员保持空曲库且开启本地自动分配。下发后确认两端曲名、各自轨道、乐器和移调都按手动方案生效，再准备演奏。随后更改同一首歌曲的分配并重新下发，确认新配置替换旧配置。最后开启自动模式并重新选曲，检查原自动流程。

已完成的检查见 STAGE-VERIFICATION.md。尚未完成本版真实多机 FF14 演奏验收。
