# 演出房间与队长接管（3.2.5.24）

本版可复用此房间执行“移动与队形”：队长下发跟随或各成员目标站位，各端开启接收后在本机执行，详见 [LOCAL-MOVEMENT.md](LOCAL-MOVEMENT.md)。移动与原自动、手动歌曲分发互相独立。

一间房间支持原房主、一名主持人和最多七个队员连接，主持人不占队员名额。主持人不需要加入演奏小队，也不需要本地 MIDI 曲库。原房主电脑保存有效队列；合奏模式由当前游戏队长执行演奏，转让队长后可以保留房间自动接管。全队和主持人须升级本版。

| 角色 | 查看当前及待演顺序 | 调整队列及控制连播 | 接收观众聊天点歌 |
| --- | --- | --- | --- |
| 当前游戏队长 | 可以 | 可以，执行演奏 | 选为接收端时可以 |
| 主持人 | 可以 | 可以，由队长执行 | 选为接收端时可以 |
| 普通队员 / 转让后的原房主 | 可以 | 不可以 | 不接收 |

## 队长电脑

1. 按 [LOCAL-SONG-SYNC.md](LOCAL-SONG-SYNC.md) 通过在线库或发布包升级，确认版本为 `3.2.5.24`，打开 MidiBard 的“自动点歌”。
2. 在“演出房间”选择“队长 / 演奏端”，本地端口默认 `28765`，点击“创建演出房间”。组队时须由小队队长的主控端创建。
3. 樱花启动器的 TCP 隧道指向 `127.0.0.1:28765`。已有隧道使用其他本地端口时，可把插件的监听端口改为相同数值。隧道运行在创建房间的电脑，转让队长后仍保留在这台电脑；不需要另开 WebSocket 服务。
4. 在插件中填入樱花分配的节点域名或 IP，以及公网端口。域名框只填主机名，不带 `http://`、端口或路径。
5. 点击“主持人邀请”旁的复制图标，把完整邀请私下交给主持人。点击“队员查看邀请”旁的复制图标，将查看邀请交给普通队员。两种邀请权限不同，不要把主持人邀请当作队员邀请分发。不要将口令、樱花访问密钥或启动器配置发布到公开仓库。

原房主电脑、FF14、插件与樱花启动器均须保持运行，原房主留在同一小队。插件只监听本机回环地址，无需把监听范围扩大到所有网卡，也无需自行放行公网入站端口。

## 主持人电脑

1. 在“演出房间”选择“主持人”，粘贴邀请口令，点击“加入演出房间”。不需要安装樱花启动器。
2. 显示“已连接队长”后，“曲库”和“点歌队列”显示队长端数据。主持人原曲库保留，离开房间后恢复显示。
3. 主持人和队长都能加歌、拖动歌曲名排序、取消排队、设为下一首、处理未匹配点歌，以及播放、暂停、继续、停止、跳过当前曲目。
4. “设为下一首”调整下一首待演曲目；拖动后清除该覆盖并采用拖动后的顺序。暂停直接暂停当前歌曲，合奏时向全队同步暂停；继续约延迟 1.8 秒同步恢复。“自动连播”独立勾选，关闭时本曲正常播完；停止会结束本轮播放，但保留连播勾选状态。

## 普通队员电脑

1. 启用本版插件，打开“自动点歌 → 演出房间”，选择“队员查看”。
2. 粘贴队长发来的“队员查看邀请”，点击“加入演出房间”。不需要樱花账号或启动器，也不需要另建隧道。
3. 连接后打开“演出列表”，查看当前曲目、下一首和待演顺序。主持人或队长加歌、排序、指定下一首或删除后，列表自动更新。
4. 跨电脑全队开启当前歌曲同步、合奏监听和 PMD，队员可以保持空曲库。关闭歌曲同步时各端仍须准备相同 MIDI。PMD 按 MIDI 文件内容校验并回报载入结果，允许列表顺序不同；同机 IPC 同步仍需同序列表。全队使用本版并开启自动分配后，插件按轨名自动配器、按队长的小队显示顺序分配演奏人，无须逐首封装。手动模式会额外下发本次选曲的演奏人、乐器和移调等设置；在原合奏面板关闭队长自动分配后编辑并点击“下发歌曲与手动分配”。歌曲缓存与配置同步不替代 PMD 合奏控制；具体限制见 LOCAL-SONG-SYNC.md。
5. 断线时保留上次同步的列表并明确提示重连；恢复后自动更新。关闭房间后需要队长重新创建并分发新邀请。

查看邀请使用独立密钥，普通查看连接不能控制队列。队员本机自动队列须先停止，正常的 MidiBard 合奏跟随播放可继续；进入查看不会主动暂停或停止它。成为游戏队长且完成身份核验的连接才会获得队列控制及演奏权限。持有查看邀请的人可加入，最多七名同时在线；只有口令而未通过队长身份核验，不能取得控制权。

## 转让游戏队长

1. 房间使用“合奏主控”，候任队长提前以“队员查看邀请”加入原房间，并开启歌曲同步。
2. 插件自动在小队频道发送 `mbstageproof` 身份验证消息。原房主根据游戏提供的真实发送角色核验连接；不按可自行填写的昵称授予权限。
3. 在游戏中转让队长，新队长连接会切换为“当前队长”，获得加歌、拖动、删除及播放控制权限。主持人继续使用原连接，无需重新邀请；原队长变为只读。
4. 正在演奏时，新队长沿用本机同一首 MIDI 的播放实例和暂停状态，不重新开曲。歌曲结束后，由新队长准备下一首。尚在载入时，旧载入取消，待演条目保留，接管后重新准备。
5. 转回原队长同样可以接管。失效的旧权限指令和旧载入回执不能再次生效。

这不是服务器迁移。原房间、有效队列及樱花隧道留在原房主电脑，原房主须继续在线且留在同一小队。新队长未连接、当前歌曲不匹配或缺少 MIDI 时，界面显示等待或失败原因，不自动删除、跳过歌曲。断线重连后会重新验证身份并尝试接管；缺曲导致载入失败时，同步 MIDI 后重新点击播放。

没有创建共享房间时，只更新各端本地合奏权限，不把不同电脑的本地队列合并。自动接管仅用于合奏模式。

远端队长接管期间，房间保持合奏模式。需要改为房间内单人演奏时，先将游戏队长转回原房主并停止当前演奏，再切换模式；也可以退出共享房间后独立使用本机单人演奏。

## 观众自动点歌

房间默认由主持人接收聊天点歌。先勾选“接收点歌”，再按需要勾选“自动连播”并点击播放。观众在主持人能收到的频道发送 `点歌 曲名`，即可在队长队列中按接收顺序等待演奏。连播开启时队列空了仍会等新歌。播放、连播和接收点歌互相独立。

在“演出房间 → 聊天点歌接收端”可切换为队长接收；同一时间只由所选的一端接收，避免两人同时听到公开聊天造成重复入队。主持人未入队时收不到小队频道，观众应在主持人能看到的说话频道或向主持人发送悄悄话。两端不用处于同一网络，但聊天可见范围仍遵循游戏规则。

主持人断线期间，队长保留管理和演奏能力；可手动切换队长接收新的聊天点歌。未收到的聊天不会事后补采集。

## 连接与数据

- 使用 TLS 加密 TCP，主持人和队员共用一条樱花 TCP 隧道。邀请包含节点地址、角色、独立随机密钥及证书指纹；连接时固定验证指纹，不绕过证书校验。修改查看邀请中的角色字段不能获得主持人权限。
- 关闭房间、重载插件或重启游戏后，旧邀请失效，需要创建房间并重新分享。地址和邀请不自动写入账号或游戏配置。
- 断线自动重连原房间并更新列表。加歌、排序、切歌等控制指令不会自动重发；未确认的观众点歌保留最多两分钟，重连时按原操作 ID 去重。
- 双方同时修改时，较旧列表发出的操作会明确拒绝并刷新列表。需要核对后再点一次，不会自动覆盖新顺序。
- 每房间最多保留 4096 条远程操作去重记录，达到上限时重新创建房间。共享快照上限 8 MB；超过上限会关闭共享，不影响队长本地数据。
- 主持人及当前队长接收歌曲名称、版本、人数组合、当前队列及相关点歌。普通队员仅接收当前节目单中进行中、待演条目及播放状态，不接收完整曲库、观众点歌记录或条目备注。失去队长权限后恢复只读快照。开启歌曲同步后，通过小队身份核验的演奏队员可以请求当前 MIDI；不会传输本地文件路径或完整演出档案。歌曲保存为独立缓存，不要求个人播放列表一致；自动配器规则见 STAGE-README.md。

## 排查连接

先检查队长房间已创建，樱花启动器显示隧道在线，本地端口和插件一致，公网域名与端口正确。确认邀请与选择的角色一致；已有七名队员时需等其他查看连接退出。如果启用了樱花 IP 访问认证，主持人和各队员还需按樱花页面完成访问认证。不要关闭插件的身份校验。

本地测试范围及结果见 STAGE-VERIFICATION.md。本轮未验证当前樱花节点的公网连通性，也未完成游戏内主持人、全队实际合奏及转让队长验收。
