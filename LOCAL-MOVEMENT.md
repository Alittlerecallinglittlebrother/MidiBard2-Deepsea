# 3.2.5.24：跨电脑移动与队形

在深海回响版的“自动点歌”窗口新增“移动与队形”页。支持队长带着不同电脑上的乐手跟随，到达场地后统一列队；可以拖动每位乐手的位置、修改朝向、绑定角色，并保存多套预设。原自动分配、手动歌曲下发保持可用。

本版已纳入在线更新。编译、网络、界面与当前游戏文件的签名检查已完成，尚未完成真实多机 FF14 移动验收，详见 STAGE-VERIFICATION.md。

## 安装与连接

1. 全队通过在线库更新至 3.2.5.24。若使用开发插件，则完整解压发布包到新目录并重新添加包内 MidiBard2.dll；停用其他版本，不要只替换一个 DLL。安装步骤见 LOCAL-SONG-SYNC.md。
2. 所有乐手加入同一个游戏小队，并进入同一区域、同一实例，靠近至彼此可见。停止演奏并收起乐器。
3. 队长创建原“演出房间”，其他演奏端使用“队员查看邀请”连接。跨公网仍需可用的 TCP 隧道。主持人角色不执行移动。
4. 每端打开“自动点歌 → 移动与队形”，勾选“允许本机接收队长移动指令”。每次插件加载后都默认关闭，换角色后也关闭。
5. 等待“队员状态”确认参与者已连接、开启接收且没有忙碌提示。房间会沿用原小队身份验证。

移动设置独立于自动分配和歌曲同步开关；不需要歌曲或曲库即可移动。演奏仍按之前的多设备演奏、合奏监听和歌曲同步设置使用。全队需要新版才能参与移动；连接不支持移动的旧房间时，本版不会发送新的移动消息。

## 跟随队长

队长点击“开始跟随队长”，其余队员在各自电脑上持续读取游戏内队长的位置并靠近，距离约 2.5 米以内停下。队长走动后继续跟随。

到达场地后，队长点击“全员停止”，等待状态停止，再下发队形。跟随期间不保持横排或弧形；首版按“先跟随、后列队”的流程工作。

## 编辑和下发队形

1. 队长点击“新建队形”，从当前小队生成初始站位；也可以用“保存当前站位”记录眼前的队形。
2. 给队形命名，使用“横排”“弧形”“双排”生成起点。
3. 在平面图上拖动编号圆点。上方为队长前方，下方表格可精确调整左右、前后和朝向；单位为米和度。
4. 每行按角色身份绑定，可以用演奏人下拉框交换两人的位置。各电脑上的小队显示顺序不影响分配。
5. 点击“统一下发队形”。每个参与者走到自己的目标位置，到位后调整朝向，队长查看回执。列队期间不继续编辑。
6. 显示“全员已到位”后，再取出乐器、下发歌曲并开始合奏。

所有站位以队长点击下发时的位置和朝向为基准。若队形中的队长自身位置不是原点，队长也会移动到该位置；下发后的目标位置保持固定。需要让队长原地不动时，把其左右和前后都设为 0。

队形自动保存至插件数据目录中的 Stage/formations-角色CID.json，并保留上一次保存的 .bak。保存文件损坏时不会覆盖原文件。换了小队成员后，可重新建立队形或用下拉框绑定当前成员。缺少已绑定成员时拒绝下发，不会按其他人的小队序号代替。

## 停止与限制

- “停止本机”立即停止，并关闭本机移动接收；再次参加需重新勾选。队长按此按钮也会发出全员停止。
- 按 Esc 或直接操作移动、转向会退出本次自动移动。其他移动插件产生的输入也优先于本模块。
- 本次指令已停止或完成后，即使再次收到旧的房间回复，也不会重新开始移动；重新启用接收会生成新的会话。
- 断线、短时房间响应超时、切图、换队长、队员变化、队长不可见、开始演奏或取出乐器时停止。某参与者停止、失联或状态不符后，房间会停止整队本次移动。
- 原房主仍需在线并留在小队中；转让游戏队长后，新队长可以通过原房间下发。
- 首版只支持地面步行或奔跑；不支持骑乘、飞行、游泳、跨图导航、自动进门或传送。
- 这是目标方向移动，不包含绕墙寻路。目标距离超过 40 米、高差超过 3 米、持续 3 秒没有进展或列队超过 30 秒会停止。
- 单个站位偏移限制为正负 20 米，最多 8 人，最多 50 套队形。预设中的参与者都必须就绪；不在预设中的当前成员不会被分配站位。
- 默认不安装或启用游戏内移动钩子；勾选本机接收后才初始化。当前版本接口未匹配时显示错误并保持停止。
- 不直接改角色坐标，不改变游戏的传统/标准移动设置，不持有系统按键，也不要求安装 BardToolbox。

## 双机游戏验收

先在空旷、平坦、无障碍的场地用两台电脑验证：

1. 两端均为 3.2.5.24，游戏中同队同场景，进入同一演出房间后打开接收。
2. 队长点跟随，缓慢走直线并转弯，观察队员是否靠近并在停步后停下。分别检查标准和传统移动模式。
3. 队员按 Esc、按移动键、点击“停止本机”，确认不会被旧指令再次带走；重新勾选后由队长重新下发。
4. 队长保存双人队形，给队员设左右偏移 2 米、朝向 90 度，停止跟随后下发，观察位置、朝向与“已到位”状态。
5. 检查角色切图、关闭房间、断网、换队长、取出乐器后能停止。遮挡或高度不同处应停止而非持续顶墙。
6. 列队后分别测试自动歌曲分发和手动歌曲分发，确认原演奏流程可用。

这些游戏验收项尚未在本次交付完成；不要把测试替身驱动、界面截图或签名匹配当作游戏移动成功的证明。
