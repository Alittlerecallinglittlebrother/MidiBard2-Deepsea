# 演奏中换乐器 3.2.5.16

此功能随 3.2.5.16 正式版发布，默认关闭；不改原有 MIDI、轨道分配或磁盘上的乐器封装。游戏内八人合奏效果仍待验收。

## 可行性结论

原代码换乐器会停止 MIDI，再收起、拿出乐器，最后从暂停位置继续。收起乐器使游戏原生合奏状态退出，原有监视逻辑还会再次暂停播放器。两者都会破坏继续演奏的时间位置。

本版保留已经同步开奏的 MIDI 播放时钟，只暂停换乐器角色的音符输出。换好并稳定 200 毫秒后，根据当前曲目的 TempoMap 选择下一可用小节；使用 MIDI 拍号和速度变化，不用固定 BPM 或墙上时间估算。重新发声时跳过已过去的音符，沿用原声部。

这实现的是插件同步续奏，不能保证保持游戏原生合奏成员状态，也不提供游戏服务器“中途重新入团”接口。游戏内其他玩家能否持续听到、各乐器切换动作是否成功、跨电脑实际听感仍须验收。

## 使用

1. 全队升级至 3.2.5.16，先停用原版或旧开发版，只加载一份 MidiBard2。
2. 在原 MidiBard 设置的“合奏设置”勾选“演奏中换乐器”。每个角色独立开启，不随同步设置强制开启。必须在开奏前设置，演奏中不可切换此模式。
3. 照常准备 MIDI、自动分配、发起游戏合奏。八个角色包括队长都保留自己的播放时钟。
4. 需要换乐器的角色在原主窗口的乐器选择器中选新乐器。当前声部暂时静音，窗口显示切换进度与恢复的小节编号，其他角色继续播放。
5. 使用插件“停止”或主控的全队停止结束演出。启用此功能后，单独退出游戏原生合奏不会停止插件时钟。

卫月开发插件入口：`%APPDATA%/XIVLauncherCN/devPlugins/MidiBard2-Deepsea-v0.12/MidiBard2.dll`。先停用旧版，再添加此 DLL；其他电脑解压完整插件包后添加其 `MidiBard2.dll`，不要只复制主 DLL。在线安装与更新使用 [仓库订阅](https://raw.githubusercontent.com/Alittlerecallinglittlebrother/MidiBard2-Deepsea/main/repo.json)。

自动点歌房间中的暂停、继续和停止仍经队长同步。暂停不触发自动恢复；继续后仍按曲目位置等待小节。当前曲目保留相同播放对象及演出记录，不因换乐器创建新的演出。

## 边界

- “下一可用小节”指换乐器成功后的下一小节。动作耗时超出最近的小节时，等待之后的小节；曲尾无完整小节时保持静音。
- 换乐器动作最多等待 7 秒。失败时该声部保持静音，曲目继续；可重新选择乐器重试。
- 连续选择以最后一次为准；停止、换歌、退队、登出或卸载会使旧换乐器任务失效。
- 通过插件选择器切换。直接使用游戏动作热栏收起乐器后，本版会将该声部静音，需要在插件中重新选择乐器恢复。
- 换乐器后的音色只用于当前曲目，下一首仍按原有轨道设置或自动分配。电吉他组内换音色沿用原有音色切换动作。
- 同曲中途切换保留原有音高、移调和轨道。乐器音域或听感差异不会自动重编曲。
- 恢复从小节边界之后的新音符开始，跨越边界、但起音已在静音期间过去的长音不会补播。
- 没有增加周期性的跨电脑校时；沿用原生合奏开奏建立的同步及原 MidiBard 延迟补偿。长时间播放的设备时钟漂移仍需听感验证。
- 各端必须提前启用。未启用的旧端仍会按游戏原生合奏结束事件暂停；本版没有强制全队开关或开演版本握手。

## 验收顺序

先用 A、B 两人短曲测试 B 换乐器、A 换乐器、同时切换和暂停后切换，再扩展到八人。确认未切换的角色不中断，恢复时不会重播曲头，结束后下一首正常。再试变拍号、连续切换、失败重试以及换乐器期间停止。

本地验证使用 DryWetMidi 真实播放时钟、纯 MIDI 节拍计算和游戏服务替身。构建通过不代表上述游戏内验收已完成。
