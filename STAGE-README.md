# midibard2-深海回响特供版

版本 `3.2.5.17`，Dalamud API 15，Windows x64 / .NET 10。作者：akira0245, Ori, Kalle, Zune, 断水剑, SevenCat。基于 `reckhou/MidiBard2` 的 `v3-api15-stable`，提交 `d8d1bd4454604cc837affd593fc3b982f33433e7`。这是深海回响定制分支，不含 OBS 联动。

2026-09-19：3.2.5.17 恢复 3.2.5.15 的演奏逻辑，移除演奏中换乐器功能并补充 SevenCat 署名。已安装 3.2.5.16 的用户可直接更新，步骤见 [安装说明](README.md)。

本版重新整理播放控制、拖动排序和共享曲库，并按音轨名字及队长的小队显示顺序自动安排合奏。欢迎语音及其资源、依赖和设置页已移除。旧版原始音频不受影响。

v0.11.3 在原 MidiBard 主窗口底部与自动点歌窗口顶部增加“联系作者”按钮，点击使用默认浏览器打开 https://shenhai.meoo.zone/ 。队长、主持人和只读队员均可使用。

保留 v0.11.2 的多设备点歌载入超时修复、按 MIDI 文件内容选曲和队员回执。共享演出房间支持游戏队长自动接管：原房间、主持人连接及队列保留，新队长负责演奏，原队长转为只读。

## 开始使用

1. 打开 MidiBard 底部的“自动点歌”。“曲库”与原 MidiBard 当前播放列表同步，可从任一界面导入 MIDI。
2. 选择“单人演奏”或“合奏主控”。单人先进入乐器演奏模式；合奏按下文配置全队。
3. 勾选“接收点歌”，观众在选定频道发送 `点歌 曲名` 后自动排队。自己加歌使用加号。
4. 勾选“自动连播”，再点击播放图标，开始顺序演奏；队列空后等待新的点歌。

点歌接收、歌曲播放、自动连播分别控制，点击播放不会打开点歌接收。

| 控件 | 行为 |
| --- | --- |
| 播放 | 开始队列；已暂停时继续当前曲目 |
| 暂停 | 暂停当前曲目；合奏时向全队同步暂停 |
| 停止 | 停止本轮播放与队列推进，保留“自动连播”的勾选状态；下次点击播放重新启动 |
| 跳过 | 跳过当前曲目，按当前播放模式处理下一首；准备期间取消尚未开始的合奏 |
| 自动连播 | 当前曲目结束后是否自动接下一首；取消勾选不会中断正在演奏的歌曲 |
| 接收点歌 | 是否接受观众新点歌，不改变当前播放 |

待演队列中按住歌曲名拖动到目标位置即可排序，队长和远程主持人都可操作。拖动会清除此前手动“设为下一首”的覆盖，后续按新顺序播放。正在准备或演奏的歌曲不能拖动。

齿轮中可设置点歌频道、口令、每人待演上限、重复点歌间隔、队列上限和歌曲间隔。同名多版本或未收录歌曲显示在“需要处理”；选择版本后直接排队。不同观众点同一首仍分别排队。

## 曲库管理

曲库每首歌旁的垃圾桶用于删除该曲目，顶部垃圾桶用于清空曲库，两者均需确认。删除同步更新原 MidiBard 当前播放列表及关联节目单条目，保留磁盘 MIDI 和演出历史。正在演奏或准备中的歌曲不能删除；清空前先停止演奏。

原 MidiBard 播放列表的增删也会同步至自动点歌曲库。首次升级时合并已有曲目；清空后不会在下次启动时把旧曲库重新导回。已有别名和编曲信息随保留的歌曲继续使用。

## 自动合奏

在原 MidiBard 合奏设置中开启自动配器及小队顺序分配（本版默认开启）。载入歌曲时自动生成本次演奏配置，无须逐首手动封装。

- 只计算实际含有音符的轨道，忽略指挥轨、空轨和仅含零力度 NoteOn 的轨道。
- 第 1 条有效轨道对应队长屏幕上小队列表第 1 位，依次最多 8 人。两轨曲目只分配前两位，其余成员收起乐器、不参与。超过小队人数的轨道不自动分配。
- 队长将同一份顺序发送给所有乐手，不使用每台电脑各自可能不同的小队排序。
- 优先按音轨名称识别乐器，例如 `萨克斯`、`Sax`、`钢琴`、`Piano`、`F-ShuangHuangGuan`。未识别时依次使用已有有效乐器配置、MIDI Program Change，最后回退竖琴。音轨名称不明确时请先检查结果。
- 保留可匹配的既有移调、速度等配置。关闭自动分配后可继续使用原有手动配置流程。自动点歌的单人模式不会套用小队分轨。

全队使用 `3.2.5.17`，开启合奏监听。同机多开开启客户端同步；不同电脑开启原 MidiBard 多设备演奏（PMD）。各端预先备好相同 MIDI，演出房间不传 MIDI。多设备模式按文件 SHA256 选曲，允许播放列表顺序不同；同机 IPC 同步仍应使用同序播放列表。队长在自动点歌中选择“合奏主控”，插件载入、分配轨道、切换乐器后发起原有准备确认。

多设备载入时队长直接读取本机文件，并等待每位队员确认载入。缺曲或读取失败会指出具体队员；超过 30 秒没有回执会列出未回应队员，取消本次推进，歌曲保留待演。旧版本不会发送新回执，因此必须全队升级。确认曲库和版本后重新点击播放，不会自动跳过失败歌曲。

队长身份每帧检查。没有共享房间时，原队长转为合奏队列只读，未完成的载入和准备取消，各电脑的本地队列不会互相迁移。要保留同一队列并自动接管，使用下文的演出房间；候任队长须提前连接房间。

“单人演奏”只控制本机，不生成小队分轨配置；即使角色在队伍中，也不会自动改成合奏。此时原生合奏窗口显示“当前曲目：单人演奏”。需要小队演奏时，先停止当前曲目，在自动点歌顶部选择“合奏主控”，对待演歌曲点击播放；需要重演已停止的歌曲时，将其重新排队。

暂停会同步到当前同一 MIDI；继续使用带时间戳的全队命令，约 1.8 秒后从暂停位置继续，迟到的端会修正位置。各电脑需保持系统时间同步。指令校验队长身份、歌曲内容哈希及先后顺序；过期或不同歌曲的指令不会执行。此机制已完成离线协议检查，真实 FF14 合奏的暂停效果、时间精度和跨网络表现仍需实机验收。

准备超过 90 秒未实际开奏时停止推进并保留待演歌曲。重载插件不会自动播放。合奏尚在准备时跳过会取消准备，避免残留倒计时启动错误曲目。

## 主持人与队员

打开“演出房间”，队长创建房间后通过现有樱花 TCP 隧道，分别分享主持人邀请和队员查看邀请。一间房间支持一名主持人与最多七个队员连接。

主持人无需入队，也无需本地曲库，可加歌、拖动排序、取消、指定下一首、播放、暂停、停止及切换自动连播；队长保留同等权限。普通队员只查看当前、下一首及待演顺序。房间连接与演奏跟随是两套功能，查看角色不替代 PMD 或客户端同步。详细步骤见 [STAGE-ROOM.md](STAGE-ROOM.md)。

在“合奏主控”模式下，候任队长通过“队员查看邀请”加入原房间。插件通过小队频道核验角色身份；转让游戏队长后，新队长自动获得队列和播放权限，主持人继续使用原连接，旧队长及普通队员只读。正在演奏的同一首歌沿用新队长本机已有播放状态，不重新载入；下一首由新队长载入、自动分配并发起合奏。载入中发生转让时取消旧操作，接管后重新准备待演曲目。

房间服务器、有效队列和樱花隧道仍留在创建房间的电脑上，原房主需要保持在线并留在同一小队。此功能交接演奏执行权，不迁移服务器。新队长未连接、缺少同一 MIDI 或验证尚未完成时暂停推进并显示原因，保留队列。全队和主持人须升级本版；各端预先备齐相同 MIDI，演出房间不传输歌曲文件。

## 节目单与演出记录

“更多”只保留“节目单”和“演出记录”，移除了点歌记录、手动控场、开演检查的界面入口。已有记录数据保留。自动演奏不要求手动创建场次或归档。

“已结束”和节目单的垃圾桶可删除单条或清理已结束节目。演出记录支持删除明细、清理本场已结束明细或删除整场；正在演奏或准备的条目受到保护。

演出记录可直接导出 JSON 或 CSV，不要求先归档。点击导出图标选择格式，结果存放到插件数据目录 `Exports`，文件夹按钮可打开。每次生成独立文件，不覆盖上一次。默认数据目录为 `%APPDATA%/XIVLauncherCN/pluginConfigs/MidiBard2/Stage`，以界面显示路径为准。

## 安装与构建

独立仓库：https://github.com/Alittlerecallinglittlebrother/MidiBard2-Deepsea 。卫月自定义插件仓库地址：https://raw.githubusercontent.com/Alittlerecallinglittlebrother/MidiBard2-Deepsea/main/repo.json 。在线安装和源码下载步骤见 [README.md](README.md)。使用本地开发版时，先停用原版或旧版插件，再在卫月开发插件中添加：

`%APPDATA%/XIVLauncherCN/devPlugins/MidiBard2-Deepsea-3.2.5.17/MidiBard2.dll`

本版与原 MidiBard2 共用内部标识和配置路径，不能同时启用多个版本。升级前备份配置；旧版开发目录保留。GitHub Release 提供对应源码 `MidiBard2-source.zip`。`MidiBard.Stage.dll` 和 `BardStage.Core.dll` 是依赖，不是独立插件入口。

安装 .NET SDK 10 和 Dalamud API 15 开发文件后执行 `./build-stage.ps1`。该脚本运行核心测试、无输出设备的真实 Playback 检查、原生 ImGui、本机 TLS、自动分配及小队载入协议检查，再编译集成插件。`./build-dev.ps1` 生成开发目录并逐文件校验；`./package-stage.ps1` 生成插件、源码、截图和 SHA256 清单。

验证范围见 [STAGE-VERIFICATION.md](STAGE-VERIFICATION.md)；状态通知接口见 [STAGE-IPC.md](STAGE-IPC.md)。游戏内加载和实际多人演奏仍需验收。

## 许可

遵循仓库 `LICENSE` 的 GNU AGPL v3 或后续版本。原 MidiBard 代码由 akira0245 编写并最初用于 MidiBard 项目，保留 Ori、Kalle、Zune 及其他贡献者署名和第三方说明。转发本地修改版时应同时提供对应源码。
