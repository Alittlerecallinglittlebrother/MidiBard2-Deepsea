# 深海回响特供版验证

## 3.2.5.16 正式版

新增默认关闭的本机开关、保持 MIDI 时钟的换乐器流程及按下一可用小节恢复的音符门控。按正式版发布，在线安装索引更新至 3.2.5.16；保留 3.2.5.15 历史 Release。此前本地实验包不覆盖，正式发布移除实验版名称与窗口标记。

2026-09-19 执行 `build-stage.ps1` 完整检查并构建：249 项核心测试通过；运行检查输出 406 条 PASS，包含 40 项换乐器专项、54 项自动分配、29 项小队播放及房间、队长接管、原生 ImGui 检查。Release 编译 41 个警告、0 个错误，版本 3.2.5.16 / Dalamud API 15。完整日志：`verification/build-experiment-final.log`。

正式发布前更新名称、状态文案及安装索引后重新构建，41 个警告、0 个错误；40 项换乐器专项再次全部通过。日志：`verification/build-release-3.2.5.16.log`、`verification/live-switch-release-3.2.5.16.log`。源码资产从发布标签对应的提交导出，插件包及源码附 SHA256 清单。

专项检查覆盖 MIDI 4/4、3/4、6/8、变拍号、变速、超时重试、连续选择、跨会话延迟动作取消、退出小队、登出、曲尾补偿缓冲等待，以及八个真实 DryWetMidi 播放实例同时换乐器而不重启时钟。

游戏动作和输出设备使用替身，实际游戏按键、延迟补偿输出和声音未验证。原生 ImGui 截图覆盖自动点歌原有界面回归，不代表新增原 MidiBard 设置开关已在游戏内验收。未在游戏内加载本版本，也未完成跨电脑八人合奏测试。

使用与边界见 [LIVE-INSTRUMENT-SWITCH.md](LIVE-INSTRUMENT-SWITCH.md)。以下为先前稳定版本的验证历史，不作为本轮新运行结果。

## 独立仓库首发

2026-09-19：将源码、安装索引和 Release 发布至 `Alittlerecallinglittlebrother/MidiBard2-Deepsea`。本次更新仓库地址、预览图地址和安装文档，C# 代码与本地 v0.11.3 保持逐文件一致。重新 Release 构建通过，43 个警告、0 个错误，输出版本 3.2.5.15 / API 15。源码包来自发布标签对应的 Git 提交；发布资产附 SHA256 清单。

下列为先前本地开发阶段的检查记录，文中的“未发布”指当时状态。本次独立仓库发布不代表新增游戏内或公网合奏验证。

## v0.11.3 联系作者入口

日期：2026-09-19。原 MidiBard 主窗口底部和自动点歌窗口顶部新增“联系作者”按钮，两处共享固定网址 https://shenhai.meoo.zone/ ，仅在点击时调用系统默认浏览器，悬停显示网址。自动点歌入口不受队列只读或演奏权限限制，空间不足时换行。

本轮构建及原生 ImGui 检查记录在 v0.11.3 的 `verification` 目录。独立源码、插件包和开发目录保留旧版，未修改个人曲库、配置或游戏启用状态；未发布 GitHub。尚未在 FF14 内加载本版，原生 ImGui 截图来自独立宿主。

- Release 构建通过，43 个警告、0 个错误。开发目录的 30 个运行及调试文件复制后逐个 SHA256 校验通过，清单版本为 3.2.5.15 / API 15。
- 接管专项运行检查 33 项通过（`verification/ui-handoff-check.log`），同时生成当前队长和只读队员的原生 ImGui 截图。已查看 760×540、140% 字体截图，新联系入口完整显示，未与原有控件重叠。
- 曲库独立检查两次在加载原生库时以 0xC0000005 退出（`ui-check.log`、`ui-check-retry.log`），不计通过；随后使用既有接管检查入口成功运行原生 ImGui。
- https://shenhai.meoo.zone/ 请求返回 HTTP 200。代码两处点击均调用系统浏览器打开同一固定网址；未在游戏中实点按钮。

以下为上一版验收记录，保留供参考，其中日志和截图位于 v0.11.2 目录，不作为本版新运行结果。本轮没有修改演奏或接管逻辑。

## 上一版 3.2.5.14 验证

日期：2026-09-19。v0.11.2，Dalamud API 15，Windows x64 / .NET 10。本轮修复多设备点歌载入超时，并增加保留原房间、主持人连接及待演队列的游戏队长自动接管。旧源码、开发插件及交付包保留，未修改用户曲库与配置，未发布 GitHub。

## 问题与修复

- 多设备选曲原先等待本机小队聊天回显执行载入。现在队长直接载入本机歌曲，再等待实际队员的载入回执；不依赖自己的聊天回显。
- 每次载入有独立请求 ID，并按 MIDI 文件 SHA256 解析歌曲，各电脑的多设备播放列表可以顺序不同。收到缺曲或读取失败回执时指出具体队员；30 秒无回执列出未回应队员，保留待演条目。旧版本不支持新回执，全队须升级。
- 队长变更时取消旧载入、旧准备及延迟发送。共享房间以小队频道的实际发送角色核验连接，只有当前游戏队长获得演奏执行权和队列控制权限，普通队员及旧队长只读。
- 房间服务器和有效队列仍在原房主电脑。主持人连接、队列条目及当前演出记录保持；当前同一首 MIDI 由新队长沿用已有播放实例，不重新载入或开曲，下一首由新队长准备。
- 接管期间取消未完成载入，接管后重新准备同一待演条目。旧权限指令、旧歌曲指令和过期回执不生效。新队长缺曲、未验证或断线时保留队列并暂停推进。
- 选择“队长接收”时，聊天点歌接收随新队长移动；选择“主持人接收”时继续由原主持人接收。新队长本机显示其执行失败的具体原因。

## 本地验证

- 核心测试：249 项通过，0 失败、0 跳过，结果在 `verification/core-tests.trx`。
- 小队载入及执行端检查：29 项通过，结果在 `verification/party-playback-final.log`。直接链接生产小队命令、队列执行端及文件身份代码，替换游戏服务；覆盖无本机聊天回显、逐员回执、冒名与旧请求拒绝、不同播放列表顺序、缺曲、并发回执、队长变更取消、延迟发送取消、同曲接管和单人播放。迟到的房间权限通知不会取消队员正在执行的新队长载入。真实等待 30 秒确认超时会列出未回应者。
- 自动分配检查：54 项通过，覆盖有效音轨、中文及英文乐器名、小队显示顺序、旧设置保留和单人载入隔离。
- 完整运行检查：281 项通过，结果在 `verification/runtime-final.log`。最终接管专项复查：33 项通过，结果在 `verification/handoff-final.log`；其中包含与完整运行检查重复的项目，不应相加。覆盖保留当前演出、主持人控制、连续转让、断线恢复、旧权限隔离和远端跳过；接管后在队长转回原房主前禁止切换单人模式，防止房间失去控制端。
- 测试使用本机真实 TLS 连接和游戏服务替身，不代表游戏内或公网验证。已检查 `verification/handoff-promoted-leader-small-140.png` 与 `verification/handoff-demoted-viewer-small-140.png`，760×540 窗口、140% 字体下新队长控制栏和普通队员只读列表可见。
- 最终 Release 构建通过，43 个警告、0 个错误，版本及清单均为 `3.2.5.14` / API 15；记录在 `verification/dev-build-final.log`。独立开发目录已生成，31 个文件复制后逐个 SHA256 校验通过；未更改游戏插件启用配置。

复现命令：

```powershell
dotnet test Stage/BardStage.Core.Tests/BardStage.Core.Tests.csproj -c Release
dotnet run --project Stage/BardStage.PartyPlaybackTests/BardStage.PartyPlaybackTests.csproj -c Release
dotnet run --project Stage/BardStage.AutoAssignmentTests/BardStage.AutoAssignmentTests.csproj -c Release
dotnet run --project Stage/BardStage.RuntimeTests/BardStage.RuntimeTests.csproj -c Release -- verification
./build-dev.ps1
./package-stage.ps1
```

## 游戏内待验收

本机检测到 FF14 进程，但未在游戏内加载本次修正版，也未完成真实小队及樱花公网隧道测试。测试截图来自独立原生 ImGui 宿主；DryWetMidi 运行检查使用无输出设备播放，不能证明游戏发声、乐器切换或多人时序。

1. 停用旧版，将卫月开发插件入口换成独立 v0.11.2 目录的 `MidiBard2.dll`，确认版本 3.2.5.14。全队及主持人均升级，保留各自配置和 MIDI。
2. 不同电脑全员开启多设备演奏和合奏监听，备齐同一份 MIDI。自动点歌选择“合奏主控”，频道点歌后播放，确认各端载入、自动分配、乐器切换及准备确认。
3. 原队长创建房间并保持樱花 TCP 隧道在线。主持人通过主持人邀请连接；候任队长通过队员查看邀请连接，完成插件自动发送的小队身份验证。
4. 演奏中转让游戏队长，确认新队长界面变成“当前队长”，旧队长只读，主持人不掉线，当前曲目不重新开始。通过主持人暂停、继续，确认操作由新队长执行。
5. 当前曲目自然结束，确认下一首由新队长载入及准备，只产生一次演出记录。再转回原队长，检查权限及后续连播。
6. 在载入期间转让队长，确认旧载入取消、同一待演条目保留，新队长完成载入后再发起准备。
7. 临时断开新队长的房间连接，再恢复，确认保留原队列并重新验证接管。某端缺少 MIDI 时应指出原因，补齐文件后重新点击播放。

原房主须保持在线并留在同一小队，本版不迁移房间服务器。没有共享房间时只更新本地合奏权限，不跨电脑迁移本地队列。

交付包不包含个人 MIDI、账号配置、观众记录、邀请口令或 Dalamud 开发库。转发修改版须提供对应源码并保留许可。
