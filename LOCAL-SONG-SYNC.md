# 3.2.5.24：跨电脑歌曲与分配同步

基于 3.2.5.21（d3df701），整合当前歌曲分发、队长手动指定演奏人、乐器和移调后一键下发，以及移动与队形，随 3.2.5.24 在线发布。Dalamud API 15，Windows x64 / .NET 10。作者顺序保持：akira0245, Ori, Kalle, Zune, 断水剑, SevenCat。

## 安装

全队与主持人刷新深海回响在线仓库并更新至 3.2.5.24。仓库地址：
`https://raw.githubusercontent.com/Alittlerecallinglittlebrother/MidiBard2-Deepsea/main/repo.json`。
使用本地测试版的用户先停用开发插件，再启用在线版本，不要同时启用多个 MidiBard2。

若继续使用开发插件，从本版 Release 下载 `MidiBard2.zip`，完整解压到新目录后重新指定其中的 `MidiBard2.dll`。不要只复制单个 DLL；其余 DLL 和语言目录也是依赖。原开发目录可保留。沿用原配置路径、曲库与已保存设置；升级前备份配置。

## 主控与演奏队员

1. 全队开启原 MidiBard 的“多设备演奏 / Play on multiple devices”和“合奏监听”。使用自动模式时全队开启“自动分配乐器与演奏人”；使用手动模式时，队长关闭它，在原合奏面板编辑并下发，队员无需更改自己的自动分配开关。
2. 打开“自动点歌 → 演出房间”，全队勾选“跨电脑当前歌曲同步”。这个新开关默认关闭，每端独立保存。原“设置 → 合奏”中的“队长通过演出房间分发当前 MIDI”是同一开关。
3. 主控作为游戏小队队长创建演出房间。现有樱花 TCP 隧道指向本机监听端口（默认 `127.0.0.1:28765`），在插件填公网节点和端口。
4. 主控将“队员查看邀请”交给演奏队员；队员选择“队员查看”加入同一房间。普通演奏队员用这个角色，主持人邀请用于不参与演奏的控场者。跨电脑仍需要已有 TCP 隧道连通，本包不提供公共中转服务器。
5. 只有主控导入 MIDI。队员可以保持空曲库，也可以保留自己顺序不同的曲库。房间连接完成后，插件通过小队消息自动确认队员身份。
6. 自动模式：主控在原播放列表双击歌曲，或在自动点歌中选择“合奏主控”并播放。手动模式：主控双击歌曲先载入本机，在原合奏面板选好每轨演奏人、乐器和移调，点击“下发歌曲与手动分配”。队员优先按文件内容查找本机曲库；缺少时接收当前歌曲，校验后载入。主控等待全体小队成员回执，再允许插件发起合奏准备。手动分配下发本身不启动演奏，成功后点击原准备按钮。详见 LOCAL-MANUAL-ASSIGNMENT.md。

“Use party chat for playlist sync”用于按位置同步列表增删，不是文件下载。各端有不同个人曲库时请关闭它，避免列表编辑同步影响个人歌曲。本功能不需要网盘或各端列表顺序一致。

## 行为与范围

- 分发当前选中的标准 `.mid/.midi`，不批量同步整个曲库。单曲上限 16 MiB。
- 队员缓存保留原文件名，放在插件配置目录的 `Stage\SongCache\歌曲SHA256\`；不导入个人曲库、不改排序、不改原文件。再次选到相同内容会校验并复用缓存。
- 自动模式分发 MIDI 本体；手动模式额外发送本次选曲的轨道启用状态、演奏人、乐器、移调和速度等演奏设置。不会整份复制旁边的 JSON、歌词或默认演奏人文件，不覆盖队员个人歌曲旁的配置。
- MIDI 下载最长 90 秒；自动模式全员载入等待最多 120 秒，手动模式最多 180 秒。失败会停止本次准备并保留队列，不跳过失败成员。查看具体队员的聊天错误与房间状态，修复后由队长重新选曲或重新下发。
- 切歌、取消、房间断线或队长变化会使旧下载失效。连接会自动重连，旧切歌指令不会自动重发。
- 房间接管保留原机制：原房主及其隧道继续在线，新队长可下载原房主队列中的下一首。正在演奏的歌曲仍要求新队长已有同一播放实例，不从歌曲中间重新下载补奏。
- 没有新一轮全员载入成功时，插件内的合奏准备入口被阻止；游戏本身手动操作不属于插件的拦截范围。
- 缓存不自动加入歌单，也不自动清空；停用插件且不再使用缓存播放时，可删除 `Stage\SongCache` 释放空间，之后选曲会重新接收。
- 保留原版演奏时序与合奏启动方式，没有加入演奏中切换乐器功能。

## 本地验证

通过核心测试、真实回环 TLS 七端独立缓存传输、内容校验、取消/断线/换队长、源文件变动拒绝、空曲库载入与全员回执门控、队长接管后下载下一首，以及原生 ImGui 房间设置交互检查。手动模式另验证了版本绑定、手动配置优先于队员自动配置、重新下发和小队变化失效。3.2.5.24 尚未进行真实多台电脑、公网链路和 FF14 合奏验收；本地测试结果见 LOCAL-VERIFICATION.md。

建议首次游戏验收用两台电脑：队员空曲库，选一首两轨 MIDI；确认队员收到相同曲名、分轨和乐器正确，再准备合奏。随后测试第二首、重选缓存歌曲、断线后重新选曲，以及保留不同顺序的个人曲库。

构建：`dotnet build Midibard/MidiBard2.csproj -c Release`。
本地打包：`./package-local-song-sync.ps1 -OutputDirectory <新目录>`。
在线发布打包：`./package-stage.ps1 -OutputDirectory <新目录>`。完整源码包含新增传输、载入与移动测试，在线 `repo.json` 为 3.2.5.24。
